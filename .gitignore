# Offline build artifacts (generated by make-offline.sh — not source)
worldcup/vendor/
worldcup/flags/
worldcup/fonts/fredoka-*.woff2
worldcup/fonts/fredoka.css
worldcup/offline-config.js
worldcup/live-scores.json

# Kiosk-only Family Sync default — contains the /exec URL + family code. NEVER commit.
worldcup/sync-config.js
*.html.bak
*.bak
.DS_Store

# local test deps (no package.json; never deploy these)
node_modules/
